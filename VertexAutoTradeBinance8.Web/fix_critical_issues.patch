--- a/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Configuration/TradingOptions.cs
+++ b/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Configuration/TradingOptions.cs
@@ -32,4 +32,12 @@
     /// </summary>
     public int StartupSubscriptionCap { get; set; } = 8;
     public bool EnableExecution { get; set; } = true;
+
+    /// <summary>
+    /// Maximum fraction of account balance used as margin for a single position.
+    /// Was hardcoded at 0.12 in RiskManager — moved here so it can be tuned
+    /// per-symbol (e.g. lower for highly volatile alts) without code changes.
+    /// Default 0.12 = 12% of balance per trade.
+    /// </summary>
+    public decimal MaxMarginPercent { get; set; } = 0.12m;
 } 
\ No newline at end of file
--- a/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Services/RiskManager.cs
+++ b/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Services/RiskManager.cs
@@ -18,7 +18,13 @@
         private readonly AiSelfLearningService _ai;
         private readonly LiquidationRiskEngine _liqRisk;
 
-        private const decimal MaxMarginPercent = 0.12m; // 12% hard cap margin
+        // MaxMarginPercent moved to TradingOptions (configurable per-symbol).
+        // The property below reads from IOptionsMonitor so changes in
+        // appsettings.runtime.json are picked up without restart.
+        private decimal MaxMarginPercent =>
+            _tradingOptions.CurrentValue.MaxMarginPercent > 0
+                ? _tradingOptions.CurrentValue.MaxMarginPercent
+                : 0.12m; // safe fallback
 
         public string? LastRejectReason { get; private set; }
         public decimal LastBalanceUsdt { get; private set; }
--- a/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/appsettings.json
+++ b/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/appsettings.json
@@ -55,7 +55,7 @@
 
   "SignalConfidence": {
     "Default": {
-      "MinEntry": 0.52,
+      "MinEntry": 0.40,
       "Bands": {
         "MediumFrom": 0.52,
         "HighFrom": 0.52
@@ -66,7 +66,7 @@
       }
     },
     "BTC": {
-      "MinEntry": 0.42,
+      "MinEntry": 0.38,
       "Bands": {
         "MediumFrom": 0.42,
         "HighFrom": 0.42
@@ -77,7 +77,7 @@
       }
     },
     "ETH": {
-      "MinEntry": 0.43,
+      "MinEntry": 0.39,
       "Bands": {
         "MediumFrom": 0.43,
         "HighFrom": 0.43
@@ -194,7 +194,8 @@
     "MinNotional": 10,
     "MinNotionalGuard": 8,
     "MinNotionalGuardPercent": 0,
-    "CooldownSeconds": 90
+    "CooldownSeconds": 90,
+    "MaxMarginPercent": 0.15
   },
 
   "Trading:BTC": {
