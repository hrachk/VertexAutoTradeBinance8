@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Live Dashboard duplicates Overview/Market — redirect to Market
        Nav.NavigateTo("/market", replace: true);
    }
}
