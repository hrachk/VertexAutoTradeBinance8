--- a/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Services/SimulatedTradeService.cs
+++ b/VertexAutoTradeBinance AI v5.0 Full Autonomous Mode/Services/SimulatedTradeService.cs
@@ -33,9 +33,16 @@
             _learningService = learningService;
             _logger = logger;
 
-            var root = Path.Combine(AppContext.BaseDirectory);
-            //cfg["SharedData:Root"]
-             //   ?? throw new InvalidOperationException("SharedData:Root not configured");
+            // Use SharedData:Root like every other cross-process file in this
+            // project (decision_trace_*.jsonl, klines_bootstrap.json, etc) —
+            // AppContext.BaseDirectory points at THIS process's own bin folder,
+            // which is wrong when the Web app (running from a different bin
+            // folder) needs to read missed_trades.json back. Falls back to the
+            // local bin folder only if SharedData:Root isn't configured.
+            var sharedRoot = cfg["SharedData:Root"];
+            var root = !string.IsNullOrWhiteSpace(sharedRoot)
+                ? sharedRoot
+                : AppContext.BaseDirectory;
 
             Directory.CreateDirectory(root);
           _filePath = Path.Combine(root, "missed_trades.json");
--- a/VertexAutoTradeBinance8.Web/Pages/Components/MarketSnapshot.razor
+++ b/VertexAutoTradeBinance8.Web/Pages/Components/MarketSnapshot.razor
@@ -3128,7 +3128,15 @@
     // lifecycle, not which list is currently being displayed).
     List<MissedTradeRecord> DisplayedSignals => _showHiddenSignals ? _recentExpiredSignals : _signals;
 
-    void ToggleHiddenSignals() => _showHiddenSignals = !_showHiddenSignals;
+    async Task ToggleHiddenSignals()
+    {
+        _showHiddenSignals = !_showHiddenSignals;
+        // Reset pager/scroll — the two lists (_signals vs _recentExpiredSignals)
+        // have independent page counts, so staying on the old _sigPage index
+        // after switching can point past the end of the new list (e.g. a
+        // stale "3/1" pager label) and leave sigCardsScroll at the wrong offset.
+        await GoToSigPage(0);
+    }
 
     // Keyed by Symbol+Time (not object identity/equality, which we don't
     // control on MissedTradeRecord). Value is the signal's own Time, used
@@ -3359,7 +3367,7 @@
     const int _sigPageSize = 6;
     int _sigPageCount => Math.Max(1, (int)Math.Ceiling(DisplayedSignals.Count / (double)_sigPageSize));
 
-    async void GoToSigPage(int page)
+    async Task GoToSigPage(int page)
     {
         _sigPage = Math.Clamp(page, 0, _sigPageCount - 1);
         try
